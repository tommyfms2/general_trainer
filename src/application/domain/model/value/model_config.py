

class ModelConfig:

    def __init__(self, batch_size: int, dim_embed: int, dim_hidden: int, epochs: int):
        self.batch_size = batch_size
        self.dim_embed = dim_embed
        self.dim_hidden = dim_hidden
        self.epochs = epochs

